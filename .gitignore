# ── Python / Django backend ──────────────────────────────
venv/
.venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
db.sqlite3
db.sqlite3-journal
*.log
.env

# ── Node / Expo / React Native (both apps) ───────────────
node_modules/
.expo/
.expo-shared/
dist/
web-build/
*.tsbuildinfo

# ── Android build artifacts ──────────────────────────────
android/build/
android/app/build/
android/.gradle/
android/.cxx/
android/local.properties
*.keystore
!debug.keystore

# ── iOS build artifacts ──────────────────────────────────
ios/build/
ios/Pods/

# ── Editor / OS ──────────────────────────────────────────
.DS_Store
.idea/
*.iml

# Note: VS Code settings are intentionally left tracked (.vscode/) — remove if undesired.
